# راهنمای ادامه پروژه آوا برای Gemini

این بسته، کد کامل نسخه فعلی سایت «دفتر بازدید مراکز مشهد» است. ابتدا کد موجود را بررسی و اجرا کن و ادامه کار را روی همین پروژه انجام بده. زبان رابط فارسی و جهت صفحه راست‌به‌چپ است. دسته‌بندی اصلی مراکز بر اساس خیابان است.

## وضعیت فعلی

- ۱۰۱ مرکز واقعی از فایل mashhad_clinic_acquisition_tracker.xlsx در lib/seed.json قرار دارند. ردیف نمونه وارد سایت نشده است. اطلاعات اولیه هر مرکز در فیلد raw نگهداری شده است.
- جست‌وجوی نام، آدرس، خیابان، پزشک و سایر اطلاعات؛ فیلتر خیابان، نوع مرکز و مرحله همکاری؛ نمایش بازدیدشده‌ها، بازدیدنشده‌ها، علاقه‌مندی‌ها و پیگیری‌ها پیاده شده‌اند.
- افزودن و ویرایش مرکز، ثبت بازدید، یادداشت، علاقه به همکاری، سطح مرکز و تاریخ پیگیری با ذخیره در پایگاه داده پیاده شده‌اند.
- برنامه روزانه بازدید، تغییر ترتیب مراکز، پیشنهاد ترتیب تقریبی بر اساس فاصله، لینک نقشه و خروجی CSV پیاده شده‌اند.
- رابط دسکتاپ و موبایل بررسی شده است. ساخت پروژه و بررسی TypeScript موفق بوده و ذخیره و بازیابی اطلاعات، ثبت مرکز و تاریخ پیگیری در محیط محلی آزموده شده‌اند. داده آزمایشی از پایگاه محلی حذف شده است.
- انتشار آنلاین و انتقال کد به مخزن میزبانی انجام نشده است. این بسته برای تحویل محلی کد تهیه شده است.

## ساختار فنی

React 19 + TypeScript + Vinext/Vite، رابط Tailwind/shadcn، سرور Cloudflare Worker و پایگاه Cloudflare D1 با Drizzle.

| مسیر | کاربرد |
| --- | --- |
| app/dashboard.tsx | رابط اصلی و تعاملات کاربر |
| app/visits.css | ظاهر فارسی، راست‌به‌چپ و واکنش‌گرا |
| app/layout.tsx | قالب و مشخصات صفحه |
| app/api/centers/route.ts | دریافت، ایجاد و ویرایش مراکز |
| lib/centers.ts | مدل مرکز، جست‌وجو، فاصله و ابزارهای مشترک |
| lib/seed.json | داده اولیه ۱۰۱ مرکز و اطلاعات اصلی اکسل |
| db/schema.ts | ساختار جداول |
| db/store.ts | اتصال و ورود یک‌باره داده اولیه |
| drizzle/ | فایل ساخت جداول و سابقه migration |
| vite.config.ts | تنظیم اجرای برنامه و اتصال D1 |
| .openai/hosting.json | تنظیم میزبانی پروژه فعلی Sites |

README.md اصلی مربوط به قالب آغازین است؛ این فایل وضعیت اختصاصی پروژه آوا را توضیح می‌دهد. پوشه examples کد نمونه قالب است و مسیر فعال سایت نیست. پوشه build کد افزونه لازم برای ساخت است و نباید با خروجی تولیدشده dist اشتباه شود.

## اجرای محلی

Node.js نسخه 22.13 یا بالاتر و npm لازم است. فرمان‌ها را داخل پوشه ava-visits اجرا کن:

```sh
npm ci
npm run build
```

برای پایگاه محلی تازه، فقط یک بار جداول را بساز:

```sh
node --import ./scripts/sites-env.mjs ./node_modules/wrangler/bin/wrangler.js d1 execute DB --local --config dist/server/wrangler.json --persist-to .wrangler/state --file drizzle/0000_magical_giant_girl.sql
```

سپس محیط توسعه را اجرا کن:

```sh
npm run dev
```

آدرس پیش‌فرض توسعه http://localhost:5173 است. برای پیش‌نمایش خروجی ساخته‌شده، npm start را اجرا و آدرس اعلام‌شده در ترمینال را باز کن. با اولین دریافت مراکز، داده اولیه به‌صورت یک‌باره وارد پایگاه می‌شود. داده محلی در .wrangler/state ذخیره می‌شود. این پوشه داخل بسته نیست؛ اجرای تازه با داده اولیه شروع می‌شود.

برای بررسی TypeScript:

```sh
npx tsc --noEmit
```

## نکات لازم برای ادامه

- این برنامه فقط یک صفحه استاتیک نیست؛ ذخیره تغییرات به API و D1 نیاز دارد. برای انتقال به میزبانی متفاوت باید اتصال پایگاه و اجرای سرور متناسب با آن تنظیم شوند.
- تنظیم فعلی .openai/hosting.json به پروژه ثبت‌شده همین کاربر مربوط است. شناسه پایگاه در vite.config.ts برای اجرای محلی placeholder است. استقرار مستقل به منابع و تنظیمات واقعی مقصد نیاز دارد.
- کنترل دسترسی تولید برای میزبانی خصوصی Sites در نظر گرفته شده؛ ورود مستقل کاربران و سطح دسترسی تیمی در خود برنامه پیاده نشده است. فایل chatgpt-auth.ts از قالب موجود است؛ وجود آن به معنی اعمال احراز هویت روی API نیست. پیش از انتشار عمومی یا انتقال به میزبان دیگر، کنترل دسترسی API را تکمیل کن.
- ترتیب پیشنهادی بر اساس فاصله مستقیم است و ترافیک یا مسیر واقعی رانندگی را محاسبه نمی‌کند. لینک چندمقصدی نقشه حداکثر ۹ مرکز دارای مختصات را در نظر می‌گیرد.
- برخی مختصات اکسل جابه‌جا یا تکراری بودند؛ مقادیر جابه‌جا عادی‌سازی و مختصات مشکوک حذف شده‌اند. اعتبار میدانی مختصات تأیید نشده است. ۹۰ مرکز مختصات قابل استفاده دارند.
- بعضی خیابان‌ها از محله استنباط شده‌اند یا نیاز به بررسی دارند؛ نشانه‌های عدم قطعیت در داده حفظ شده است. شناسه منحصربه‌فرد بر اساس ردیف منبع ساخته شده، زیرا شناسه‌های اکسل تکراری بودند.
- اطلاعات مراکز، از جمله تلفن و آدرس، داخل lib/seed.json همین بسته موجود است. فایل اکسل اصلی و فرم‌های Word جزو این بسته کد نیستند.
- فرم‌های کاغذی جلسه اول و جلسات بعدی در درخواست قبلی تهیه شده‌اند؛ همه فیلدهای آن فرم‌ها لزوماً ورودی مستقل قابل ویرایش در سایت ندارند. برای توسعه فرم جلسات، مدل و فرم فعلی را با نیاز کاربر تطبیق بده و ادعای پوشش کامل بدون بررسی نکن.
- خروجی فعلی CSV است؛ ورود مجدد اکسل، کار آفلاین، تاریخچه مستقل هر جلسه و ورود چندکاربره قابلیت تکمیل‌شده محسوب نمی‌شوند.

## خواسته اصلی کاربر

کاربر می‌خواهد مراکز را از راه‌های مختلف جست‌وجو و دسته‌بندی کند، بر اساس خیابان تصمیم بگیرد بازدید را از کجا شروع کند و بعد کجا برود، بازدیدشده یا بازدیدنشده بودن را علامت بزند و مرکز جدید اضافه کند. حفظ سادگی استفاده در موبایل و زبان فارسی اولویت دارد. پیش از افزودن قابلیت‌های تازه، از کاربر جهت ادامه را بگیر.

وابستگی‌های نصب‌شده، خروجی ساخت، تاریخچه Git، داده آزمایشی محلی و فایل‌های محیطی در ZIP قرار نگرفته‌اند. package-lock.json و همه کدهای پروژه، تنظیمات و migrationها همراه بسته هستند.
